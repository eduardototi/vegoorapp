<div>
  <%= react_component("Login", prerender: false) %>
  <%= react_component("RodapeLogin", prerender: false) %>
</div>

<!--
<div class="container logn-in">
  <div class="logn-in-header">
    < image_tag 'logo.jpg', width: '80px' %>
    < image_tag 'sf6.jpg', width: '100px' %>
  </div>
  < simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      < f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      < f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      < f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      < f.button :submit, "Entrar", class: "button bg-dark" %>
    </div>
  < end %>
</div>
-->
